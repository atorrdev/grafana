apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: bolsa
    user: grafana
    password: grafana
    url: http://localhos:8086
    jsonData:
      httpMode: GET